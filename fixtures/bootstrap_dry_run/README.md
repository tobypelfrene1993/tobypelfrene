# Bootstrap Dry Run Fixture

This fixture demonstrates the template workflow end-to-end.

It contains two snapshots of the same sample project:

- `bootstrap/` - initial baseline generation
- `operating/` - post-transition steady-state mode

Both snapshots are intentionally small and generic. They exist to prove the
template can initialize a repo, preserve truth, and flip modes cleanly.
