# Inherited Project

This repository appears to be a Node app, but the deployment notes mention
Python workers and the compose file references PostgreSQL and Redis.

Known claims from the inherited docs:

- "Production is stable" according to `docs/STATUS.md`
- "Migration to Kubernetes is complete" according to `docs/ARCHITECTURE.md`
- "Next.js frontend only" according to `package.json`

Reality is unclear. The docs contradict one another and the state files are missing.
