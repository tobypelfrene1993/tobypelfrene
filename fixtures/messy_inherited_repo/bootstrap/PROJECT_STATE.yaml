# PROJECT_STATE.yaml - Structured current truth

metadata:
  updated_at: 2026-03-18T19:00:00+01:00
  updated_by: agent
  version: "statedd-template-v3"

workflow:
  repo_mode: bootstrap
  bootstrap:
    completed: false
    completed_on: null
    system_investigated: true
    repo_investigated: true
    user_intake_complete: false
    unknowns_remaining:
      - primary user still needs direct confirmation
      - first milestone still needs direct confirmation
      - deployment target may differ from inherited docs

verification_labels:
  observed: verified directly in the current session
  unknown: not yet determined from currently available evidence
  reported: supported by prior evidence, not re-verified
  blocked: verification attempted but prevented
  assumed: temporary working assumption
  stale: previously verified but no longer fresh
  invalid: known false or superseded

current_state:
  repository:
    canonical_path: fixtures/messy_inherited_repo/source
    path_status: observed
    branch: inherited-import
    head: null

  operating_mode:
    status: observed
    mode: bootstrap
    summary: |
      This fixture models an existing repo adopted into bootstrap mode. It
      should not flip to operating mode until the contradictory inherited docs,
      runtime assumptions, queue, and evidence trail are all reconciled.

  project:
    name: Inherited Project
    type: adopted_existing_repo
    lifecycle_stage: bootstrap
    truth_summary: bootstrap_adoption_baseline

  environment:
    status: observed
    host:
      os: Fedora Linux 41
      kernel: Linux 6.x
      shell: /bin/bash
      terminal: xterm-256color
    package_managers:
      - unknown
    language_runtimes:
      python: unknown
    container_tooling:
      podman: unknown
      docker: unknown
    browser_tooling:
      chrome: unknown
    ports:
      listeners: unknown
      notes: |
        This fixture captures adoption output only. Runtime listeners were not
        inspected.

  repository_structure:
    status: observed
    top_level_entries:
      - NOTES.txt
      - README.md
      - docker-compose.yml
      - docs
      - package.json
    manifests:
      - package.json
      - docker-compose.yml
    entrypoints:
      - package.json:scripts.dev
      - package.json:scripts.start
    test_setup:
      - unknown
    deployment_assumptions:
      - docker compose present
    contradictions:
      - docs mention kubernetes while docker compose files remain present
      - docs claim frontend-only scope while the repo still contains multi-service runtime files
      - stability is reported in inherited docs but not yet directly verified

  evidence:
    status: active
    ledger: docs/EVIDENCE_LOG.md
    artifact_root: docs/evidence
    standard: browser_verification_or_test_output_for_user_facing_claims

  documentation:
    status: observed
    primary_user_guide: README.md
    live_docs:
      - README.md
      - AGENTS.md
      - STATUS.md
      - PROJECT_STATE.yaml
      - PROJECT_DNA.yaml
      - NEXT_ACTIONS.md
      - BACKLOG.md
      - WORKLOG.md
      - docs/EVIDENCE_LOG.md

active_problems: []
