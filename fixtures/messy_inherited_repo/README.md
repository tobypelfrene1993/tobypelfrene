# Messy Inherited Repo Fixture

This fixture simulates an inherited repository with contradictory signals.

It is split into:

- `source/` - the noisy inherited repo as found
- `bootstrap/` - the bootstrap snapshot produced from that noise

The goal is to prove bootstrap mode stays honest when the repo is ambiguous.
